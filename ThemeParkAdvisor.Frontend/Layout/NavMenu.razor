<div class="top-row ps-3 navbar navbar-dark shadow-sm" style="background: linear-gradient(90deg, #761FC7, #812BBA);">
    <div class="container-fluid">
        <a class="navbar-brand fw-bold text-dark" href="/" style="font-size: 1rem;">
            🎡 Theme Park Advisor
        </a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass nav-scrollable" style="background-color: #343a40;">
    <nav class="flex-column py-3">
        <div class="nav-item px-3 mb-2">
            <NavLink class="nav-link d-flex align-items-center text-light" href="/" Match="NavLinkMatch.All">
                <i class="bi bi-house-door-fill me-2"></i> Domů
            </NavLink>
        </div>
        <div class="nav-item px-3 mb-2">
            <NavLink class="nav-link d-flex align-items-center text-light" href="/themeparks">
                <i class="bi bi-building me-2"></i> Zábavní parky
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link d-flex align-items-center text-light" href="/attractions">
                <i class="bi bi-star me-2"></i> Atrakce
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
